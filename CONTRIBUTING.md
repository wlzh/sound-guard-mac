# Contributing

当前版本为开发预览，后续变更应同步版本化需求、技术设计、测试报告与发行说明。

提交前运行 `zsh scripts/test-all.sh` 与 `git diff --check`。独立 GuardTests 可执行程序提供状态机与注入服务测试，不依赖本机缺失的 XCTest。系统音频适配层本身仍需硬件验收。

禁止加入任何自动恢复或提高系统音量的逻辑。不得通过麦克风推断扬声器是否播放。系统状态未知时应暂停归零操作，不可猜测或将读取失败当作静音。

禁止提交真实设备标识、音频、个人路径、访问令牌、签名证书。性能数据须记录硬件、系统、构建模式、状态和测量时长。

发行前保持 VERSION、BUILD_NUMBER、App 元数据、关于窗口、CHANGELOG、版本化文档、测试报告一致。Git tag、GitHub Release、构建产物与实机通过必须分别核验，不能互相替代。
