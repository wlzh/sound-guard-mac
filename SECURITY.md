# Security

v0.3.9 增加有界本机事件诊断，最多 64 条，权限 0600。只记录阶段、时间、错误文本、重试次数及人工核对前状态；不保存音频、设备 UID、播放应用、窗口信息或原音量。文件位于用户缓存目录，不上传；退出后可删除。诊断写入失败不能影响保护。

首版仅在本机读取输出状态及调整默认输出设备音量，不使用麦克风、不保存录音、不联网、不收集遥测。用户主动点击网站/仓库会交给默认浏览器打开。开启静音流检测会读取系统音频样本，内存即时分析后丢弃。

播放恢复提醒默认关闭。开启时保存的设备与各通道音量只存在当前进程内存，退出、睡眠、关闭功能或设备变化即失效，不写入磁盘。辅助功能仅用于读取播放 App 活动窗口位置以选择显示器；关闭功能时不请求，拒绝授权时回退到鼠标所在屏幕。播放进程、窗口标题和位置均不上传、不记录日志。

声音守卫是降低意外出声概率的便利工具，不是声音隔离或防泄漏安全边界。确认恢复的是输出设备系统音量，可能使多个正在播放的 App 同时出声。应用启动之前、被退出之后、系统状态异常时、外部软件自行调大音量时，也可能出声。重要场合应手动确认静音。

不支持软件音量控制的 HDMI、数字接口及部分外接设备必须明确提示，不得假装已归零。设备切换与设置写入间存在系统竞态，须尽量复核，不能宣称系统级原子控制。

当前支持最新开发预览版。普通缺陷可提交 GitHub Issue；安全问题请先通过作者网站联系维护者约定私密披露渠道，不在公开 Issue 上传漏洞利用细节、个人音频、设备序列号或令牌。项目暂不承诺固定响应时限。
