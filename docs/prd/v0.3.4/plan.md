# v0.3.4 实施计划

## 已完成

- [x] 定义 500 毫秒至 30 秒范围与 2 秒默认值。
- [x] 增加兼容旧 JSON 的毫秒偏好字段。
- [x] 为默认和严格恢复路径增加持续播放确认。
- [x] 增加中断、配置变更及旧任务失效处理。
- [x] 设置页支持毫秒、秒和小数秒输入。
- [x] 菜单状态和只读诊断增加确认状态。
- [x] 增加偏好、控制器与 UI 自动化测试。
- [x] 同步 PRD、设计、上下游、性能和用户说明。

## 发布门禁

- [x] 统一测试、文档、版本和 diff 检查通过。
- [x] v0.3.4 原生预览生成并人工检查。
- [x] Universal 打包、strict 验签和路径隐私门禁通过。
- [x] `/Applications` 本地替换、重启和只读状态通过；首次 Core Audio 回收延迟已记录。
- [ ] 提交、推送、CI、Release 和远端摘要通过。

## 实机缺口

- [ ] 默认模式使用真实播放器验证 500 毫秒、2 秒和 30 秒。
- [ ] 严格模式使用真实有声、短静音和持续静音验证。
- [ ] 多显示器、VoiceOver、辅助功能允许与拒绝路径人工验收。
