# v0.3.9 界面设计

沿用原生菜单与品牌视觉。重建期间保留“正在重新核对”标题；自动恢复显示“检测暂时中断，自动重试 N/3”，手动核对显示等待资源释放。冷却期间禁用重复核对，避免用户重复触发。

达到上限或不可重试错误显示“保护需要检查”及错误内容，允许人工重新核对。帮助子菜单新增“显示诊断文件…”，在 Finder 定位本机 JSON。诊断不自动弹窗、不发声、不新增焦点干扰。

恢复音量面板不改布局和按钮；重建期间关闭旧面板，原音量仅在有效记录仍保留时可供后续用户确认，绝不因恢复检测而自动出声。
