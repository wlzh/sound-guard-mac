# v0.3.8 / build 13

恢复音量弹窗新增滑块，默认保持归零前原值，可选择 1–100%、步进 1%。滑动和还原不立即出声，只有点击“恢复到 N%”才写系统音量。未调整时精确还原原始快照，调整后保留声道比例。

面板为 420×350pt，两个底部按钮仍为 185×34pt，百分比与确认文案同步。拖动暂停倒计时、松手继续；保持静音、暂时关闭和超时行为不变。新弹窗不记住上次选择，设备变化不能绕过安全复核。

不新增权限、Tap、存储或后台轮询。MIT 开源，仍为 ad-hoc 签名开发预览，未经公证。自动化和真实设备验收边界见[测试报告](../TESTING.md)。
