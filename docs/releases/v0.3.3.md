# v0.3.3 / build 8

恢复提示现在有两个明确且等宽的选择：“保持静音，不再提醒”会结束本次自动归零恢复监测并保持音量为 0；“恢复音量到 N%”会在设备、路由、静音和音量复核通过后恢复归零前的各通道音量。

左上角关闭与倒计时不代表用户已经作出选择，只临时收起窗口；当前播放停止、随后重新开始时仍可再次提示。倒计时和关闭按钮提示文字同步说明此行为。

音频检测方式、默认关闭的恢复提醒、辅助功能申请时机和隐私边界不变。macOS 14.2+，Universal arm64/x86_64，MIT License；ad-hoc 签名，未公证。
