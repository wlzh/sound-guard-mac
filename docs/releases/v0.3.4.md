# v0.3.4 / build 9

恢复提醒新增独立的“有声确认”设置，默认 2 秒，可设置 500 毫秒至 30 秒。新的播放活动必须持续满足确认时长才显示恢复提示；播放停止或确认时长变化会取消未完成候选并重新计时。

默认模式只建立一个可取消的单次截止任务，不新增周期轮询。开启“静音流也计时”后继续复用既有 Signal Tap 和 0.5 秒检查，不新增音频访问或权限。自动归零、恢复前安全复核、显式保持静音、临时关闭和倒计时语义不变。

macOS 14.2+，Universal arm64/x86_64，MIT License；ad-hoc 签名，未公证。
