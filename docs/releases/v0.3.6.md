# v0.3.6 / build 11

修正严格静音流检测的启动配置：旧配置会等待播放源输出音频后才启动，因此没有播放时反而报“没有有效回调”并停止自动归零。改为不等待播放源，保留异常安全检查与单次重试机制。

v0.3.5 的冷却方案没有解决根因，不能视为成功修复。新增有效样本诊断字段、配置回归和零音量下三轮只读重建探针；本机已观察严格空闲 1 分钟后自动归零。其他设备、权限及长期性能仍需验收。

macOS 14.2+，Universal arm64/x86_64，MIT，ad-hoc 签名、未公证。
