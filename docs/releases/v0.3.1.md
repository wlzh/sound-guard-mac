# v0.3.1 / build 6

修复开启“静音流也计时”时，自动归零后原有持续静音输出进程会立即触发“检测到新的播放”的误报。

严格模式现在在恢复等待阶段复用既有 Signal Tap，并以真实数字信号从静音变为有声作为提示条件；默认模式继续使用低开销进程事件。信号检测失败会撤销恢复凭据并保持音量为 0，不影响已经完成的自动归零。

设置、恢复窗口、辅助功能申请时机、逐通道音量恢复及默认关闭策略不变。macOS 14.2+，Universal arm64/x86_64，MIT License；ad-hoc 签名，未公证。
