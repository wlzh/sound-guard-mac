# Changelog

## [0.3.6]

- 修正严格检测聚合设备的 `TapAutoStart` 配置：原先为 true 会等待播放源提供音频，无播放时收不到回调并被健康检查判为故障；改为 false 后不再等待播放源。
- v0.3.5 的 3 秒冷却未解决根因，撤回“资源竞争已修复”的结论；保留单次重试与取消保护，不再以延长等待替代配置修复。
- 只读状态新增 `signalHasFreshSamples`，区分 Tap 对象存在与最近确实收到有效样本。
- 增加配置回归和零音量下三轮只读重建探针，不保存录音、不写音量、不改变偏好。

## [0.3.5]

注：以下为当时修复假设，实机复测仍失败；真实配置根因及修正见 v0.3.6。

- 修复“重新核对”立即销毁并重建 Signal Tap 时的 Core Audio 资源竞争，避免无回调后锁存故障并停止自动归零。
- 手动重新核对改为先完整释放音频检测，冷却 3 秒后只重建一次；期间显示“正在重新核对”，设备回调、设置刷新和重复点击不能提前重建。
- 重新核对期间禁用同名菜单项；退出和睡眠会使待执行重试失效，唤醒仍立即重新建立完整保护周期。
- 增加重试冷却、重复命令、回调隔离、退出、睡眠和唤醒回归测试；不引入常驻轮询或新的权限。

## [0.3.4]

- 新增独立“有声确认”时长，默认 2 秒，可设置 500 毫秒至 30 秒；与弹窗停留时长互不影响。
- 恢复提示仅在播放活动连续达到确认时长后出现；播放停止、设备或设置变化会取消或重置未完成确认。
- 默认模式使用单个截止定时任务，不新增轮询；严格模式复用现有 Signal Tap 与 0.5 秒检查，不新增音频访问。
- 状态与只读诊断可区分等待播放和正在确认，新增旧定时任务失效、边界、迁移、重置与设置 UI 测试。

## [0.3.3]

- 明确恢复提示的互斥选择：“保持静音，不再提醒”会消费本次自动归零恢复凭据，“恢复音量到 N%”仍在安全复核后恢复。
- 左上角关闭与倒计时仅临时收起；保持音量为 0，停止播放后再次开始仍可提示。
- 两个不同长度的按钮继续使用 185×34pt 等宽对齐矩形，主次操作、无默认回车键和品牌视觉不变。
- 显式保持静音后立即释放恢复监听；增加状态消费、错误请求、回调分流、关闭与超时回归测试。

## [0.3.2]

- 修复 Release 二进制包含本机源码绝对路径的问题；打包前移除调试符号并强制执行本机路径隐私门禁。
- 统一静音流、播放恢复、错误和许可证提示窗口的声音守卫品牌图标，启动时也显式设置 App 图标。
- 重排恢复声音浮层：强化“是否恢复”问题、播放来源、归零前设备与音量、倒计时和操作层级。
- 两个操作按钮改为等宽等高并与信息卡片对齐；恢复按钮仍无 Return 默认快捷键，避免误操作。
- UI 门禁新增品牌 Alert、面板尺寸、信息层级和按钮对齐检查。

## [0.3.1]

- 修复开启“静音流也计时”时，自动归零后把原有持续静音输出进程误判为新播放并立即弹出恢复提示的问题。
- 严格模式归零后复用既有 Signal Tap，以真实的静音到有声信号沿触发提示；默认模式仍使用低开销播放进程事件。
- 严格恢复检测失败时撤销临时恢复凭据并保持音量为 0，不影响已完成的自动保护。
- 新增持续静音、恢复有声和恢复信号故障隔离回归测试。

## [0.3.0]

- 新增默认关闭的“播放时提示恢复”：只在声音守卫自动归零后监听新的播放活动。
- 弹窗默认 60 秒，可配置 5–600 秒；保持静音、关闭和超时均不恢复，只有明确点击才恢复。
- 保存并恢复各输出音量通道原值；恢复前复核设备 ID、UID、路由、音量和静音状态，失败时保持或回滚到 0。
- 通过 Core Audio 播放进程定位 App；辅助功能只在用户开启功能后请求，拒绝时回退鼠标所在显示器。
- 增加设置、菜单、明暗弹窗、旧偏好迁移、播放沿、失效与故障隔离测试。

## [0.2.2]

- 只修正设备/0% 行下方的多余空白：头部 84pt 改为 72pt，其他文字、原生菜单和音频机制不变。
- 新增明暗外观末行底部留白不超过 8pt 的检查，UI 检查共 20 次。

## [0.2.1]

- 菜单顶部从 106pt 收紧至 84pt，品牌 12pt、主状态 14pt，设备信息 12pt，音量独立右对齐。
- 长名称和错误摘要单行截断，保留完整提示和辅助功能文字；未知音量不显示百分比。
- 增加明暗外观布局和长名称回归检查，UI 检查增至 18 次。
- 音频检测、偏好结构、默认行为和权限不变，不增加后台刷新。

## [0.2.0]

- 公开 GitHub 仓库，保留 MIT 开源协议。
- 盾牌与声波品牌图标，菜单栏单色模板及彩色 App 图标。
- 重设计菜单、常规与设备设置、关于和独立许可证面板。
- 新增状态展示回归、12 项 UI 检查和模拟设备截图；音频检测机制不变。
- 继续作为开发预览，保留 v0.1.0 硬件及长期性能验证缺口。

## [0.1.0] - 2026-09-10

- 首个开发预览，build 1，原生 Swift/AppKit 菜单栏 App。
- 无输出播放活动默认 5 分钟后单向归零，支持 1–120 分钟和暂停/恢复。
- 默认仅内建扬声器，其他输出设备以稳定 UID + 路由白名单勾选。
- 可选数字静音流检测，默认关闭；音量为 0 或已静音时释放检测资源。
- 归零前复核、写后回读、故障停止、睡眠/设备切换重置、单实例锁。
- 设置、登录启动、关于、完整 MIT 协议、离线文档、统一自动化测试与 CI。
- 预览限制：系统音频权限、外接设备矩阵、长时驻留尚需完整实机验证；无 Developer ID 公证。
